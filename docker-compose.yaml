services:
  backend:
    container_name: "listgen"
    image: "listgen"
    build:
      context: .
    restart: unless-stopped
    expose:
      - 5000
    ports:
      - "5000:5000"
    environment:
      TEMPLATE_DIR: "/app/templates"
